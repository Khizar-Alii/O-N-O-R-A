import React from "react";

const HCHelpWrapper = (props) => {
  return (
    <div
      style={{
        margin: "20px 0",
      }}
    >
      {props.children}
    </div>
  );
};

export default HCHelpWrapper;
